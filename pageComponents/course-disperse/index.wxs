
var inArray = function (dataId, dataArray) {
  var index = -1
  dataArray.forEach(function(item, i) {
    if(item.data_id == dataId) {
      index = i
    }
  })
  return index != -1
}

module.exports = {
  inArray: inArray
}
